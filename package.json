{
  "name": "bike-manager",
  "private": true,
  "version": "0.1.0",
  "description": "The bike manager!",
  "repository": "https://github.com/amplitudesolutions/bike manager",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.9.0",
    "shelljs": "^0.7.5"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 443",
    "pretest": "npm install"
  },
  "dependencies": {
    "bower": "^1.7.9",
    "lodash": "^3.10.1"
  }
}
